---
title: Color Picker
description: An input wrapper for react-colorful's color picker.
createdAt: 2025-04-11
updatedAt: 2025-04-11
---

<References
  links={[
    {
      href: 'https://omgovich.github.io/react-colorful/',
      label: 'React Colorful',
    },
  ]}
/>

<ComponentPreview
  name='color-picker-demo'
  v0Url='https://kelvinmai.io/r/color-picker.json'
/>

## Installation

<Tabs defaultValue='cli'>

<TabsList>
  <TabsTrigger value='cli'>CLI</TabsTrigger>
  <TabsTrigger value='manual'>Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

<NpmCommand value='npx shadcn@latest add https://kelvinmai.io/r/color-picker.json' />

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies</Step>

<NpmCommand value='npm install next-themes lucide-react motion clsx tailwind-merge' />

<Step>Add a classname utility function</Step>

```ts
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export const cn = (...inputs: ClassValue[]) => {
  return twMerge(clsx(inputs));
};
```

<Step>Copy and paste the following code into your project</Step>

<ComponentSource name='color-picker' title='components/color-picker.tsx' />

<Step>Update the import paths to match your project setup</Step>

</Steps>

</TabsContent>
</Tabs>

## Usage

```ts
import { ColorPicker } from '@/components/ui/color-picker';

export default function ColorPickerDemo() {
  const [color, setColor] = React.useState('#fff');
  return <ColorPicker value={color} onChange={setColor} />;
}
```

## Props

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Name</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Required</TableHead>
      <TableHead>Default</TableHead>
      <TableHead>Description</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell>
        <InlineCode codeblock={{ value: 'options', lang: 'ts' }} />
      </TableCell>
      <TableCell>
        <InlineCode codeblock={{ value: 'string[]', lang: 'ts' }} />
      </TableCell>
      <TableCell>No</TableCell>
      <TableCell>
        <InlineCode codeblock={{ value: 'PRESET_COLORS', lang: 'ts' }} />
      </TableCell>
      <TableCell>
        An array of hex code colors, the preset uses 8 tailwind colors.
      </TableCell>
    </TableRow>
  </TableBody>
</Table>
